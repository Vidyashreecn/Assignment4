//Write a function declaration named calculateBMI that takes two parameters: weight (in kilograms) and height (in meters). The function should return the Body Mass Index (BMI) calculated using the  formula: BMI=height2weight

function calculateBMI(weight, height) {
  return height * weight;
}
console.log(calculateBMI(20, 10));
