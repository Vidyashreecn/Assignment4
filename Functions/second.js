//Create a function expression named concatStrings that takes two string parameters and returns their concatenation.

const concatStrings = function (str1, str2) {
  return str1 + str2;
};

console.log(concatStrings("Hello, ", "Welcome"));


